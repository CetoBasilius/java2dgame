package com.java2dgame.resources;

import org.junit.Test;

public class TestResourceLoader {
	
	@Test
	public void LoadRxesourceListTest() {
		
		//resourceList = XmlHelper.loadXMLFile(ResourceLoader.class,RESOURCELIST_FILENAME);
		
		//XmlHelper xmlHelper = createNiceMock(XmlHelper.class);
		
		
		
		//expect(XmlHelper.loadXMLFile(ResourceLoader.class, ResourceLoader.getResourcelistFilename()));
		//expect(nodeList.getLength()).andReturn(5);	
		
		//replay(xmlHelper);
		
		//ResourceLoader.getInstance().loadResourceList();
		
		//verify(xmlHelper);	
	}

}
